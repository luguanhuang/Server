#include "pch.h"
#include "role/ptcm2g_synguildinfo.h"
#include "unit/role.h"
#include "unit/rolemanager.h"
#include "guild/guildrecord.h"

// generate by ProtoGen at date: 2016/8/31 17:53:19

void PtcM2G_SynGuildInfo::Process(UINT32 dwConnID)
{
	/*
	Role* pRole = RoleManager::Instance()->FindByRoleID(m_Data.roleid());
	if (pRole != NULL)
	{
		CGuildRecord* pGuildRecord = pRole->Get<CGuildRecord>();
		if (pGuildRecord != NULL)
		{
			pGuildRecord->SetGuildId(m_Data.guildid());
		}
	}
	*/
}