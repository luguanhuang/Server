#include "pch.h"
#include "common/ptcm2t_versionnotify.h"
#include "config.h"

// generate by ProtoGen at date: 2016/7/27 21:24:57

void PtcM2T_VersionNotify::Process(UINT32 dwConnID)
{
	//Version& roVersion = GateConfig::Instance()->GetVersion();
	//roVersion.Clear();
	//roVersion.type = m_Data.type();

	//std::stringstream ss;
	//ss <<"set version by ms, type:"<<roVersion.type;

	//for (int i = 0; i < m_Data.ios_size(); ++i)
	//{
	//	roVersion.ios.insert(m_Data.ios(i));
	//	ss <<" ios:"<< m_Data.ios(i);
	//}
	//for (int i = 0; i < m_Data.android_size(); ++i)
	//{
	//	roVersion.android.insert(m_Data.android(i));
	//	ss <<" android:"<<m_Data.android(i);
	//}

	//SSInfo<< ss.str().c_str() <<END;
}
